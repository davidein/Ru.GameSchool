@{
    ViewBag.Title = "Index";
}

<h2>Listi yfir próf</h2>

@foreach (var item in ViewBag.Exams)
{
    <ul>
        <li>@Html.ActionLink((@item.Name + " - Skiladagur: "), "Get", new { LevelExamId = @item.LevelExamId } )</li>
    </ul>
}
